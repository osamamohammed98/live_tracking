name: live_tracking
description: A new Flutter application.
publish_to: 'none'
version: 1.0.0

environment:
  sdk: ">=2.7.0 <3.0.0"

dependencies:
  flutter:
    sdk: flutter

  location: ^3.0.2
  google_maps_flutter: ^1.0.2

flutter:
  uses-material-design: true

  assets:
    - assets/car_icon.png